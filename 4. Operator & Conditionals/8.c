#include <stdio.h>
int main()
{
  int n;
  printf("Enter the value of n: ");
  scanf("%d", &n);

  if (n % 2 == 0)
    printf("The number is -1");

  else
    printf("The number is 1");

  return 0;
}